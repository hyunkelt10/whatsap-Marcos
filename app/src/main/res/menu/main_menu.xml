<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- action button for search -->
    <item android:title="search"
        android:id="@+id/search"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"
        android:icon="@drawable/search_icon"/>

    <!-- action button for refresh -->
    <item android:title="Nuevo grupo"
        android:id="@+id/newGroup"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <!-- action button for copy -->
    <item android:title="Nueva Difusión"
        android:id="@+id/newDifusion"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <!-- action button for copy -->
    <item android:title="Dispositivos Vinculados"
        android:id="@+id/vinculatedDisp"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <!-- action button for copy -->
    <item android:title="Mensajes Destacados"
        android:id="@+id/msgFeatured"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <!-- action button for copy -->
    <item android:title="Ajustes"
        android:id="@+id/settings"
        android:orderInCategory="100"
        app:showAsAction="never" />
</menu>